# How does a thin wire support this many connetions worldwide?

### For a while I wasn't even able to comprehend that millions of data traveling to and fro at the same millisecond without colliding but now its way better. This is how all of it works:
- First of all the data we send travels in an optical fibre cable which has number of strands of glasses.
- How does this glass contain data? Just like how data can be converted into 0's and 1's in form of electricity the same happens here using wavelength-division multiplexing
- This is actually division of data into multiple colors with 100+ different wavelenghts.(Sooo cool right?)
- But how can this data not collide with other's data which is also traveling within same underwater cable? This actually is prevented by converting our data which is in colors into small packets.
- These packets are transfered one at a time-interval that respects other data's too.
- So the prblem solved!


# How does Cloudfare Warp stop your ISP?

### The question might seem very easy but think about it, when you download a file from a website how does it encrypt that? It seems very intimidating but the answers is simple.
- It is very interesting. Warp uses a protocol called WireGuard.
- What Warp does is it encrypts ur data using advanced cryptography Which prevents ISP from seeing your data and ur data travels both the times to warps server so it encryps ur downloaded data even before it reaches the ISP.

# Now the real ques is how does the data know where to go?
- The thing is, the data we send or recive changes back and forth to electricity and color. Why is that? Its due to the routers placed at evry junction.
- The routers read the data in 1's and 0's ans send it forward to its location or simmply our systems.

# Atlast, How does a VPN work?
- To be honest I think its already answered.
- When a data leaves your system the VPN app encrypts your data and sends it to the VPN pservice provider's server where your IP is set to the server IP of server your data went to.
- Like if I set my VPN to Australia then my data will got to a server of Australia and get its IP changed to that places IP.
- And now the server sends this to your web browser where it interprets your data and takes your changes IP and thinks the request came directly from that server not you.
